# Database Configuration
# SQL Server connection string
DB_URL=jdbc:sqlserver://localhost:1433;databaseName=ContactManagementDB;encrypt=true;trustServerCertificate=true

# Database credentials
DB_USERNAME=your_database_username
DB_PASSWORD=your_database_password

# JWT Configuration
# Secret key for JWT token signing (minimum 256 bits required)
JWT_SECRET=your-secret-key-here-minimum-256-bits-required-for-HS512
JWT_EXPIRATION=86400000

# Instructions:
# 1. Copy this file to .env (do not commit .env file)
# 2. Replace all placeholder values with your actual credentials
# 3. For JWT_SECRET, generate a strong random string (at least 32 characters)
# 4. JWT_EXPIRATION is in milliseconds (86400000 = 24 hours)
